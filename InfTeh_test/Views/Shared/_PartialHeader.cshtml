<link href="@Url.Content("~/Content/css/MainPage.css")" rel="stylesheet" />
<script>
    $(document).ready(function () {
        OpenCookieUl();
        $('ul li a').click(function (e) {
            var parent = this.closest('ul');
            console.log(parent.id);
            SaveState(parent.id);
        });
    });
</script>

<a class="navbar-brand" href="#">ТЕХСА <span style="color:#f3a23f">Workbook</span></a>
<button class="navbar-toggler"
        type="button"
        data-toggle="collapse"
        data-target="#navbarCollapse"
        aria-controls="navbarCollapse"
        aria-expanded="false"
        aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
</button>

<div class="collapse navbar-collapse" id="navbarCollapse">
    <nav class="navbar navbar-expand-lg navbar-dark fixed-top" style="background: #343a40;">
        <a class="navbar-brand" href="#">ТЕХСА <span style="color:#f3a23f">Workbook</span></a>
        <button class="navbar-toggler"
                type="button"
                data-toggle="collapse"
                data-target="#navbarCollapse"
                aria-controls="navbarCollapse"
                aria-expanded="false"
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="navbar-nav mr-auto sidenav" id="navAccordion">
                <li class="nav-item">
                    @Html.ActionLink("✨ Главная", "Index"
                        , new { Controller = "Home", Area = "" }
                        , new { @class = "nav-link" })
                </li>
          
                <li class="nav-item">
                    <a class="nav-link nav-link-collapse"
                       href="#"
                       id="PlacesSubItems"
                       data-toggle="collapse"
                       data-target="#collapsePlaces"
                       aria-controls="collapsePlaces"
                       aria-expanded="false">📌 Локации</a>
                    <ul class="nav-second-level collapse" id="collapsePlaces" data-parent="#navAccordion">
                        <li class="nav-item">
                            @Html.ActionLink("📋 Все локации", "Index"
                                , new { Controller = "Places", Area = "" }
                                , new { @class = "nav-link" })
                        </li>
                        <li class="nav-item">
                            @Html.ActionLink("🏘 Типы локаций", "Index"
                                , new { Controller = "PlaceTypes", Area = "" }
                                , new { @class = "nav-link" })
                        </li>
                    </ul>
                </li>
            </ul>
        </div>
    </nav>
</div>


